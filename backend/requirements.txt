# ICT AI Trading System - Backend Requirements
# 100% FREE - No paid APIs required!

# ============================================
# 🚀 PERFORMANCE OPTIMIZATIONS
# ============================================
orjson>=3.9.0               # 6x faster JSON serialization
uvloop>=0.19.0              # 2-4x faster async event loop (Apple Silicon)
polars>=0.20.0              # 10-100x faster than pandas for data ops
aiohttp>=3.9.0              # 10x faster concurrent HTTP (also used for Telegram)
faster-whisper>=1.0.0       # 4-8x faster transcription
mlx-vlm>=0.1.0              # 5-7x faster vision (native Apple Silicon Metal GPU)

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database (SQLite - FREE, no server needed)
# SQLite is built into Python - no additional package needed!

# Video Processing (FREE)
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.0  # FREE transcript extraction
pytubefix>=8.0.0              # Reliable YouTube audio download (handles 403 errors)

# AI/ML (FREE - local processing)
numpy>=1.26.0
pandas>=2.2.0               # Keep for compatibility, use polars for heavy ops
scikit-learn>=1.4.0
joblib>=1.3.0

# Frame Deduplication (FREE - for smart training optimization)
opencv-python>=4.9.0    # Used for perceptual hashing & scene detection

# Market Data (FREE - Yahoo Finance)
yfinance>=0.2.36

# Chart Generation (FREE)
matplotlib>=3.8.0

# Notifications (FREE - Telegram)
aiohttp>=3.9.0

# Scheduler (FREE)
apscheduler>=3.10.0

# HTTP Client
httpx>=0.26.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.7

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Logging
loguru>=0.7.0

# ============================================
# Vision Training (FREE on Apple Silicon!)
# ============================================
# Local vision uses Ollama (install from https://ollama.ai)
# Then run: ollama pull llava
# No additional Python packages needed - uses HTTP API
requests>=2.31.0         # Already included, for Ollama API calls

# ============================================
# OPTIONAL - Only if you want to use paid APIs
# ============================================
# anthropic>=0.18.0       # Claude API (paid)
# openai>=1.10.0          # OpenAI API (paid)
# pinecone-client>=3.0.0  # Vector DB (paid)
# twilio>=9.0.0           # WhatsApp (paid)
# torch>=2.2.0            # PyTorch (heavy)
# transformers>=4.37.0    # Hugging Face (heavy)
